#pragma once

constexpr unsigned char CELL_SIZE = 8; // Размер каждой ячейки в игровой сетке.

constexpr unsigned char COLS = 10; // Количество столбцов в игровой сетке.

constexpr unsigned char ROWS = 20; // Количество рядов в игровой сетке.

constexpr unsigned char SCREEN_RESIZE = 4; // Фактор, с помощью которого изменяется размер экрана.

constexpr unsigned short FRAME_DURATION_MS = 750; // Длительность каждого кадра в миллисекундах